extends base
    
block content

    main.main
        .review-form
            -console.log(tour)
            h2.heading-secondary Leave a Review
            p.heading-small.ma-bt-sm= `For ${tour.name} Tour`
            form.form.form--review(data-tour-id=`${tour.id}`)
                .form__group
                    label.form__label(for='rating') Your Rating
                        div#rating.star-rating
                            - for (let i = 1; i <= 5; i++)
                                span.star(data-value=i) ★
                .form__group
                    input#rating.form__input(type='hidden', value=0, required)
                .form__group
                    label.form__label(for='reviewText') Your Review
                    textarea#reviewText.form__input(name="review" placeholder="Write your review...")
                .form__group
                    button.btn.btn--green Submit Review